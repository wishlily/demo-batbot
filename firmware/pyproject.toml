[project]
name = "firmware"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "catkin-pkg>=1.1.0",
    "colcon-common-extensions>=0.3.0",
    "lark-parser>=0.12.0",
]
